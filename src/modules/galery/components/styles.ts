import styled from 'styled-components';

export const Container = styled.div`
    .carrousel {
        display: none;
        @media (max-width: 480px) {
            display: block;
        }
    }

`;